#include<iostream>

using namespace std;

int main() {
	const int length = 6;
	int arr[length]{ 98,632,92,3,64,-1 };

	/*for (size_t i = 0; i < 69; i++)
	{
		cout << arr[i] << ' ';
	}
	cout << '\n';*/

	/*for (int item : arr) {
		cout << item << ' ';
	}
	cout << '\n';*/

	/*
		Iterators:
		input - vxodniye iteratori
				dlya chteniya adresuemix dannix iz pamyati
			==, !=, *i, ++i, i++, *i++
		output - vixodniye iteratori
				addresuyut danniye dlya zapisi v pamyat
			++i, i++, *i = value, *i++ = value

		forward - odnonapravlenniye iteratori
				soderjit vse svoystva input i output iteratorov
				i moqut peremeshatsya ot nacahala posledovatelnosti
				do konsa
			==, !=, *i, ++i, i++, *i++
		biderectional - soderjit vse svoystva input i output iteratorov
				i moqut peremeshatsya ot nacahala posledovatelnosti
				do konsa i naoborot
			==, !=, *i, ++i, i++, *i++, --i, i--, *i--
		random access - iteratori proizvolnoqo dostupa
				soderjit svoystva vsec chetirex iteratorov
				i moqut peremeshatsya ot nacahala posledovatelnosti
				do konsa i naoborot
			==, !=, *i, ++i, i++, *i++, --i, i--, *i--
			i+=n, i+n, i-=n, i-n, i1 - i2, i[n],
			i1<i2, i1<=i2, i1>i2, i1>=i2

	*/
	
	
}