#include<iostream>

using namespace std;

int main() {
	// Ternary ?: - condition ? if_true : if_false;
#pragma region Ternary
	//int numb{};
	//cout << "Enter number: ";
	//cin >> numb;

	//// positive negative
	///*if (numb >= 0) cout << "Number is positive!" << endl;
	//else cout << "Number is negative!" << endl;*/

	///*numb >= 0 ? cout << "Number is positive!" << endl: cout << "Number is negative!" << endl;*/

	///*cout << (numb >= 0 ? "Number is positive!" : "Number is negative!") << endl;*/

	//int modulo{};
	///*if (numb >= 0) modulo = numb;
	//else modulo = -numb;*/

	//modulo = (numb >= 0) ? numb : -numb;


	//cout << "|" << numb << "| = " << modulo << endl;

#pragma endregion

#pragma region Nested ternary
	//int numb{};
	//cout << "Enter number: ";
	//cin >> numb;
	///*if (numb == 0) cout << "Nol" << endl;
	//else if (numb % 2 != 0) cout << "Nechotnoye" << endl;
	//else  cout << "Chetnoye" << endl;*/

	//cout << (numb == 0 ? "Nol" : numb % 2 != 0 ? "Nechotnoye" : "Chetnoye") << endl;

#pragma endregion



}